import { VexConversationView } from "../vex/VexConversationView";

/** `/vex` — conversa em tela cheia dentro do shell (o painel lateral não abre aqui). */
export function VexPage() {
  return <VexConversationView variant="page" />;
}
